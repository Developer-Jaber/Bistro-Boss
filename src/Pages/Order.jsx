import Coverd from "../Shared/Coverd";
import bannerImg from "../assets/shop/banner2.jpg"

const Order = () => {
    return (
        <section>
            <section>
                <Coverd bg={bannerImg} heding={'OUR SHOP'} subHeding={'Would you like to try a dish?'}></Coverd>
            </section>
        </section>
    );
};

export default Order;